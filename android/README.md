# My Sales Android

Native Android wrapper for `https://mysales.frappe.cloud/`.

Build a debug APK with Android SDK 35 and Java 11:

```bash
./gradlew assembleDebug
```

The APK is generated at `app/build/outputs/apk/debug/app-debug.apk`.
